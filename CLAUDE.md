# CLAUDE.md — Etihad B2B Cabinet

## Что это
B2B-кабинет для агентств-партнёров туроператора Etihad (он же Turon Tour). Агентства бронируют пассажиров на заезды через аэропорт Трабзона (TZX) или Батуми (BUS), оператор видит все брони и управляет оплатами.

Две роли: `agency` (бронирует, видит свои брони) и `operator` (видит всё, управляет агентствами).

## Стек
- **Frontend**: чистый HTML/CSS/JS, без сборки, GitHub Pages
- **Backend**: Cloudflare Workers + D1 (SQLite)
- **Demo-режим**: если `js/config.js` пустой — данные в localStorage

## Файловая структура

```
index.html              — каталог + вход + кабинет (одна страница, 3 экрана)
styles.css              — светлая/тёмная темы, [hidden]{display:none!important}
img/                    — фотографии (hero, отели); в CSS только ссылки, не base64
js/config.js            — TURON_CONFIG.apiBaseUrl (пусто = демо)
js/seed-data.js         — заезды, туры, направления (генерируется!)
js/api.js               — API-клиент + демо-fallback на localStorage
js/catalog.js           — публичный каталог (направления → туры → карточка)
js/provisional.js       — ВСЕ неподтверждённые данные (рейсы, ночи в отелях, контакты)
js/app.js               — экраны агентства, переключение экранов
js/admin.js             — экраны оператора

worker/index.js         — весь бэкенд (маршруты, бизнес-логика)
worker/wrangler.toml    — конфиг деплоя, ALLOWED_ORIGIN сюда

db/schema.sql           — таблицы БД (для новой базы)
db/seed.sql             — данные (генерируется build-seed.py)
db/migrations/          — ALTER-ы для уже созданных баз, применять по одному разу

tools/extract-departures.py  — парсит ведомость.xlsx → seed/departures.json
tools/build-seed.py          — departures.json → db/seed.sql + js/seed-data.js
tools/build-preview.js       — собирает preview.html (однофайловое превью)
tools/check-seed.py          — сверяет db/seed.sql с генератором (см. подводные камни)
tools/seed-i18n.py           — переводы наполнения (uz/en/tr), ключ = русская строка
tools/check-i18n.py          — ловит непереведённые строки и осиротевшие ключи
tools/build-i18n-migration.py — переводы для ЖИВОЙ базы (017), сид её не трогает

seed/departures.json    — заезды и цены (источник для сборки)
test/api-test.mjs       — 135 интеграционных тестов (сервер)
test/media-test.mjs     — хранилище фотографий: обе ветки, R2 и KV
test/ui-test.mjs        — 475 дымовых тестов интерфейса на preview.html (playwright)
test/run.sh             — запуск тестов (wrangler dev + reset DB + тесты)
test/reset-db.sh        — пересборка тестовой БД начисто (пароль turon2026)

karadeniz-tour-info.md  — программа тура Карадениз (источник контента карточки)
mir-jahon-spec.md       — разбор платформы-референса + что у нас есть/нет
ЧТО-УТОЧНИТЬ.md         — для отдела: что предварительное и какие вопросы к оператору
```

## Таблицы БД (schema.sql)

```
agencies        — логин, хэш пароля, роль (agency/operator), активен
sessions        — токены сессий
login_attempts  — для rate limiting (8 попыток / 25 с IP за 15 мин)
tours           — каталог туров: комиссии, description, nights
departures      — заезды (дата, аэропорт TZX/BUS, capacity, seats_taken)
departure_prices — цены: взрослые по размещению (SGL/DBL/TPL/PROM) + детские
bookings        — брони (agency_id, departure_id, статус, сумма, оплачено)
passengers      — пассажиры брони (ФИО, ДР, паспорт, тип, occupies_seat)
payments        — оплаты и возвраты
booking_events  — журнал: created/edited/cancelled/payment (actor_name + actor_role)

destinations    — оформление плитки направления, ключ = tours.destination (текст!)
tour_variants   — варианты маршрута тура (A/B у Карадениза)
tour_content    — контент карточки: included/excluded/info/gallery/day
```

`destinations.name` связан с `tours.destination` по тексту, а не по FK —
намеренно, чтобы не переписывать все запросы, читающие destination строкой.
Направление без строки в `destinations` не теряется: показывается по названию.

## Ключевые правила бизнес-логики

**Места НЕ ограничивают продажу.** Потолок `capacity` из запроса УБРАН —
вместимостью управляет оператор вручную (позвонит агентству, если мест нет),
`seats_taken` ведётся только для сводок и с реальностью не сверяется:
`UPDATE departures SET seats_taken = seats_taken + ? WHERE id = ? AND is_open = 1`.
Единственная блокировка — закрытый заезд (`is_open = 0`). Остаток мест агенту
не показывается (`app.js`, departuresBlock) и гостю отдаётся ведром «20+».
**Не пишите здесь «защита от двойной продажи» — её нет и не задумано.**

**Продажу заезда останавливает `is_open`** — и это единственный рычаг
оператора над продажей, раз потолок мест снят. Кнопка «Закрыть продажу» на
панели над списком пассажиров (`#adm-dep-controls`), маршрут
`POST /api/admin/departures/:id/(open|close)`. Закрытие мягкое: проданные
брони живут дальше, ведомость печатается, оплаты проводятся — не создаются
только новые брони и не правится состав.

**Удаления заезда нет и не планируется.** `departure_prices` ссылается на
заезд с `ON DELETE CASCADE`, а `bookings` — без него: удаление снесло бы
весь прайс, а брони остались бы без заезда. Закрытие продажи решает ту же
задачу и ничего не теряет.

**Ловушка, на которой уже подорвались:** `listDepartures` фильтровала
`is_open = 1` ДЛЯ ВСЕХ. Этой же функцией ищут заезд `createBooking` и
правка состава — то есть фильтр и ЕСТЬ запрет продажи. Но если прятать
закрытые ото всех, оператор, закрыв заезд, терял бы его из кабинета совсем
и открыть обратно было бы нечем. Теперь у неё третий параметр
`includeClosed`: продающие пути его НЕ передают (и закрытых по-прежнему не
видят), а `/api/departures` передаёт только оператору.

**Та же мина этажом выше — `tours.is_bookable`.** Тур, снятый с продажи,
уводил за собой ВСЕ свои заезды, включая операторский список: снял тур
галочкой — и ведомости, цены и кнопка «открыть продажу» по его заездам
стали недоступны. Пока туры заводились только seed-файлом, галочку никто
не трогал и мина молчала; ожила вместе с вкладкой «Туры». Теперь обе
проверки (`is_open` и `is_bookable`) снимаются одним `includeClosed`.

**В превью это не воспроизводится:** демо-заезды не связаны с турами, и
`departures()` там про `is_bookable` не знает вовсе. Серверные фильтры
доступа проверяются чтением SQL, а не прогоном демо-режима.

**Закрытая продажа запрещает новые МЕСТА, а не правку проданного.** Убрать
отказавшегося туриста надо уметь и на полном рейсе — это место освобождает.
Раньше `adminUpdateBookingPassengers` и `updatePassengerBirthdate` искали
заезд без `includeClosed` и отбивали ЛЮБУЮ правку на закрытом; оператор
открывал продажу, правил и закрывал обратно, а в этот промежуток заезд
снова принимал брони. Настоящий запрет живёт в `UPDATE departures ... AND
is_open = 1`: добавить человека на закрытый заезд по-прежнему нельзя.

Панель управления лежит ОТДЕЛЬНЫМ блоком над ведомостью, а не на карточке
заезда: карточка это `<button>`, вложить в неё вторую кнопку нельзя.

**У демо-заездов проставляется `id`** (`withDemoIds` в `js/api.js`). В
seed-данных его нет — только `code`, а `id` появляется лишь в базе
(AUTOINCREMENT). Маршруты вроде «открыть/закрыть продажу» адресуют заезд
по id, и без этого кнопка молча слала `NaN`. Backfill идёт и для уже
сохранённого состояния: в localStorage демо-пользователя лежит слепок
без id.

**Новый заезд создаётся ПО ОБРАЗЦУ** — `POST /api/admin/departures`,
кнопка «+ Новый заезд» над сеткой заездов. Прайс лежит не у тура, а у
КАЖДОГО заезда отдельно (3-6 размещений плюс детские), и заводить их
руками на каждую пятницу сезона нереально — двенадцать заездов по семь
полей. Поэтому указывается заезд-образец, и его прайс копируется целиком;
отличия правятся кнопкой «Цены».

Руками остаётся одна дата: аэропорт и вместимость подтягиваются с образца,
а код собирается как «аэропорт + ДДММ» (`BUS2808`) — той же схемой, что в
ведомости. Как только оператор тронул поле кода, подсказка перестаёт его
перетирать (`newDepCodeTouched`).

**Без образца заезд создаётся СРАЗУ ЗАКРЫТЫМ** (`is_open = 0`). Это нужно
новому туру, где копировать неоткуда, но продавать без цен нечего: открытый
пустой заезд агентство увидело бы в списке и уткнулось в «нет цены на
размещение». Оператор заполняет прайс и открывает продажу сам.

Проверки: код 3-24 знака, дата существует (`invalidCalendarDate` — тот же
разбор обратно в строку, что у даты рождения, иначе 31 февраля станет
3 марта и пройдёт), дата не в прошлом, код не занят, вместимость целая от 1,
образец существует.

`invalidCalendarDate` живёт отдельно от `invalidBirthDate` намеренно: тому
нужны границы «не в будущем» и «не раньше 1900», а датам заездов будущее
как раз норма. Общая у них только проверка существования — и она, как и
всё остальное, продублирована в `js/api.js` для демо-режима.

**Туры заводит оператор** — вкладка «Туры», `POST /api/admin/tours` и
`POST /api/admin/tours/:id`. Тур это ПРОДУКТ, заезды — его даты; раньше
туры попадали в базу только seed-файлом, то есть руками через код.

**КОД ТУРА ПОСЛЕ СОЗДАНИЯ НЕ МЕНЯЕТСЯ.** Он стоит в публичном адресе
карточки (`#/t/KARADENIZ`), и агенты рассылают такие ссылки клиентам —
смена кода тихо ломает все уже отправленные. Поле при правке заблокировано,
и сервер отклоняет попытку отдельно: убрать поле из формы мало.

**Удаления тура нет**, как и удаления заезда: на него ссылаются заезды, а
через них брони. Вместо этого `is_bookable = 0` — тур пропадает из каталога
и из продажи вместе с предстоящими заездами, и ответ сервера сообщает,
сколько их скрылось (`hidden_upcoming`).

Направление подсказывается уже заведёнными (`datalist`): плитки каталога
связаны с турами ПО ТЕКСТУ направления, и «Турция » с лишним пробелом
завела бы вторую плитку-дубликат.

Цены у тура НЕТ — она на заезде. После создания тура интерфейс об этом
прямо напоминает, иначе оператор ищет поле цены и не находит.

**Контент карточки правит оператор** — кнопка «Карточка» у тура,
`GET/POST /api/admin/tours/:id/content`. Программа по дням, «включено /
не включено / информация» и варианты маршрута.

Набор строк заменяется ЦЕЛИКОМ, как прайс заезда и состав брони: не
остаётся висящих строк и не нужны маршруты на добавление и удаление.

**Порядок задаётся ПОЗИЦИЕЙ строки**, `sort` проставляет сервер по ней.
Числа сортировки руками не правятся — на них ошибаются: два блока с
«сорт 3» дают случайный порядок, и причину не найти. В интерфейсе поэтому
стрелки вверх/вниз, а не поле с числом.

Дни привязываются к варианту маршрута (`variant`); день без варианта общий
для всех. Ловушка, закрытая тестом: добавленный вариант должен СРАЗУ
появиться в выпадающих полях у всех дней (`refreshDayVariants`), сохранив
уже выбранное, — иначе день терял бы привязку молча, а сервер отбивал бы
сохранение целиком, и оператор не понял бы за что.

Сервер не даёт: пустую строку, выдуманный `kind`, день с несуществующим
вариантом (такой день просто не показался бы в карточке — молчаливая
потеря данных), два одинаковых варианта, вариант без заголовка.

**Ловушка имён блоков:** `#oc-variants` и `#oc-days` во множественном
числе, остальные по виду (`#oc-included`). Формулой это не выводится —
`ocBoxId()` держит карту явно. На выводе формулой уже подорвались: кнопка
«+ Вариант» искала несуществующий `#oc-variant` и падала молча.

**Перенос заезда на другую дату** — `POST /api/admin/departures/:id/date`,
кнопка «Изменить дату». Работает в два шага: без `confirm` только СОБИРАЕТ,
кого заденет, и ничего не пишет.

Дата тянет за собой больше, чем кажется: сроки оплаты считаются ОТ ДАТЫ
ВЫЕЗДА, а за `FINAL_DAYS` до него и ближе платить надо всё сразу и отмена
удерживает 100%. То есть перенос ближе к сегодняшнему дню может задним
числом загнать в штрафную зону бронь, которую агентство оплатило наполовину
и отменить теперь может только с потерей всей суммы. Такие брони считаются
отдельно и подписываются красным — агентство об этом не просило.

**Сроки на сервере НЕ считаются намеренно.** Правило живёт в одной
`TuronApi.paymentPolicy`, и вторая точка правды рано или поздно разойдётся
с первой. Сервер отдаёт факты (дата брони, сумма, оплачено), а предпросмотр
считает та же функция, что рисует «Платежи» агентству.

Подписи в таблице пишутся ПО ФАКТУ, а не одной фразой: если обе даты уже
внутри `FINAL_DAYS`, оплата считается от даты брони и от переноса не
зависит — там честно «срок не меняется», а не выдуманное «сроки
сдвигаются».

Перенос пишется в журнал КАЖДОЙ затронутой брони: агентство иначе увидит
уехавшие сроки платежей и решит, что это сбой.

**Прайс заезда правит оператор** — `POST /api/admin/departures/:id/prices`,
кнопка «Цены» на той же панели. Набор строк заменяется ЦЕЛИКОМ (как состав
брони): так не остаётся висящих строк и не нужны отдельные маршруты на
добавление и удаление.

**На проданные брони правка цен не влияет и влиять не может** — цена
пассажира заморожена в `passengers.price` в момент брони и из
`departure_prices` больше не перечитывается. Это не оговорка, а ПРИЧИНА,
по которой правку вообще можно давать оператору: иначе смена цены задним
числом меняла бы суммы у клиентов, которым уже выставлен счёт. В самом
редакторе это написано словами — без этой строки оператор боится трогать
цены и звонит уточнять.

Чего сервер не даёт: пустой прайс, прайс без единого размещения (заезд
нечем продавать), цену не числом или отрицательную, детский диапазон
«от 10 до 5», два одинаковых кода. Отдельно — **убрать тариф, по которому
уже едут** (409): сама строка сумму брони не держит, но подписывает тариф
в ведомости и билете, а без неё в списке пассажиров остался бы код,
которому в прайсе ничего не соответствует.

Возрастной диапазон и «занимает место» активны только у детского тарифа,
у размещения гасятся: иначе оператор впишет «от 5 до 10» строке `DBL` и не
поймёт, почему это ни на что не влияет.

**Цена пассажира**: по возрасту на дату ВЫЕЗДА (не брони). Если возраст в диапазоне детского тарифа — детская цена, иначе взрослая по типу размещения. Граница «от и до, не включая верхнюю»: тариф 5-10 → с 5 до 9 лет 11 мес.

**Младенцы** (до 2 лет): `occupies_seat = 0`, цена берётся, но место не тратится.

**Комиссия**: фиксируется на момент брони в долларах ЗА КАЖДОГО ТУРИСТА,
занимающего место (подтверждено оператором; младенец без места комиссию
не приносит — см. seatsNeeded в createBooking). Операторская — сверху агентской, агентству не видна.

**Прошедшие заезды**: агентству недоступны (`date_start >= date('now')`), оператору — с `?all=1`.

**Отмена — ТОЛЬКО оператор.** `POST /api/bookings/:id/cancel` отвечает агентству
403: убрать кнопку мало, токен у агентства есть и маршрут остался бы открыт по
API. Агентство шлёт заявку `POST /api/bookings/:id/cancel-request` — она ничего
не отменяет, только пишет `cancel_requested` в журнал брони и зовёт оператора в
Telegram (тот же `TELEGRAM_BOT_TOKEN`, что и у новых броней). Отменяет оператор
через `POST /api/admin/bookings/:id/cancel` — там нет фильтра по `agency_id`,
поэтому функция живёт только в ветке `/api/admin/`. Возврат мест прежний
(обратный UPDATE), статус `cancelled`.

**Опечатка в паспорте ≠ изменение брони.** `POST /api/admin/passengers/:id/document`
правит ТОЛЬКО ФИО, номер и срок паспорта: ни `placement`, ни `birth_date`, ни
цена не трогаются, поэтому сумма и число мест измениться не могут в принципе.
Отдельно от `updateBookingPassengers`, который заменяет состав целиком С
ПЕРЕСЧЁТОМ, — чинить опечатку через пересчёт брони опасно. Только оператор
(решение оператора: агент ошибку внёс, исправляет туроператор — по нему же
выписан билет). В журнал пишется «было → стало», иначе спор «я так не писал»
не разрешить. Кнопка — в «Списках пассажиров», где оператор и так смотрит
ведомость глазами; ведомость поэтому отдаёт `passenger_id`.

**Дата рождения — НЕ опечатка, а пересчёт.** По ней определяется тариф
(детский/взрослый по возрасту на дату ВЫЕЗДА), а у младенца до 2 лет ещё и
`occupies_seat = 0` — значит меняются цена пассажира, сумма брони, число
занятых мест и комиссия агентства. Поэтому она НЕ входит в правку документа
и живёт в своём `POST /api/admin/passengers/:id/birthdate`.

**Обе правки живут в одном окне `#pax-modal`** («Изменить» в «Списках
пассажиров»), а не в трёх подряд `prompt()`, как было сначала: системные
окна не показывают расчёт, не дают вернуться на шаг назад и на части
телефонов вообще отключены. Формы внутри окна ДВЕ и разделены намеренно —
документ сохраняется сразу, дата рождения только после «Рассчитать».

Ловушка, закрытая тестом: расчёт устаревает, если поправить дату уже после
него. `dropBirthPreview()` на `input` гасит и предпросмотр, и кнопку
«Применить», а сама кнопка шлёт `paxEdit.previewDate`, а НЕ значение поля —
иначе применилось бы не то, что оператор видел на экране.

Окно лежит в `<body>` рядом с `#screen-app` (как и окно брони — иначе
`position:fixed` считался бы от экрана), поэтому тёмную палитру кабинета
оно само не получает: она скоупнута на `:root[data-app-theme="dark"]
#screen-app`. Для `#pax-modal` тот же блок переменных продублирован
отдельно, вместе с самим `color` — иначе на тёмном кабинете всплывала бы
кремовая плита.

Маршрут работает **в два шага**: без `confirm` он только СЧИТАЕТ и возвращает,
что изменится (тариф, цена, мест, сумма брони), ничего не записывая; с
`confirm` — применяет. Так оператор не подписывается вслепую под сменой суммы.
Предпросмотр считает ту же `priceFor`, что и запись, поэтому показанное и
сделанное разойтись не могут — дублировать тарифную логику в интерфейсе не
пришлось.

`keep_price: true` оставляет прежнюю цену пассажира при смене тарифа: ошибку
внёс агент, и оператор может решить деньги не двигать (решение оператора —
выбирает в момент правки). Дельта мест проводится тем же порядком, что в
`updateBookingPassengers`: сначала занимаем место с проверкой `is_open`, потом
пишем пассажира, при сбое место возвращаем — иначе счётчик заезда «уедет».

**Замена СОСТАВА — тоже только оператор.** `POST /api/bookings/:id/passengers`
отвечает агентству **403**: маршрут был открыт агентству, и это была та же
дыра, что с отменой — замена туриста это деньги (взрослый/детский тариф) и
место, на которое может быть уже выписан билет. Агентство шлёт заявку
`POST /api/bookings/:id/change-request` — она ничего не меняет, пишет
`change_requested` в журнал и зовёт оператора в Telegram. Проводит замену
`POST /api/admin/bookings/:id/passengers`.

Что оператор УЖЕ мог без этого маршрута: переписать пассажира целиком —
ФИО и паспорт через `/document`, дату рождения через `/birthdate`. То есть
«вместо Иванова едет Петров» работало и раньше. Новый маршрут закрывает
ровно три дыры, которых не было ни в одной из тех форм: **добавить**
человека, **убрать** человека и **сменить размещение**. Они и есть
рискованные — меняют число мест и комиссию.

Работает **в два шага**, как правка даты рождения: без `confirm` только
СЧИТАЕТ и возвращает «было → стало» (туристов, мест, сумму, комиссию),
ничего не записывая; с `confirm` — применяет.

`keep_price: true` оставляет прежние цены строк (решение оператора: ошибку
внесло агентство, деньги можно не двигать). Работает ТОЛЬКО при неизменном
числе туристов — цены переносятся по позициям, и при разном количестве
переносить их не с чего. Иначе сумма брони разошлась бы с суммой строк, а
счёт на оплату печатает и то и другое: клиент увидел бы таблицу, которая
не сходится с итогом. Тариф (`price_code`) при этом ставится НАСТОЯЩИЙ —
в ведомости и билете должно стоять, кем турист летит на самом деле;
заморозить можно только цену.

**Открытая заявка на замену считается иначе, чем на отмену.** Отмену
оператор закрывает сменой статуса брони, и достаточно проверить, что
события ещё нет. Замену он закрывает ПРАВКОЙ, а бронь остаётся
`confirmed` — то есть по одной брони заявок за сезон может быть несколько.
Поэтому открытой считается заявка НОВЕЕ последнего `edited`
(`change_requested_at` считается подзапросом с этим условием в
`listBookings` и `adminBookings`): пока оператор не тронул состав,
повторная кнопка не плодит сообщения, а после правки агентство может
попросить снова.

**Форма замены — та же самая форма брони** (`#booking-modal`,
`openBooking(code, booking)`), а не своя копия у оператора: тарифы,
подсказки, список размещений и расчёт суммы должны считаться в одном
месте. Но живёт она в `js/app.js`, а операторские экраны — в `js/admin.js`,
который грузится РАНЬШЕ и о ней ничего не знает. Поэтому связь идёт
колбэком: `TuronAdmin.setCompositionOpener(fn)` вызывает app.js при старте.
Обратное направление невозможно по порядку загрузки скриптов.

Расчёт рисуется в `#bm-preview` внутри того же окна, и там та же ловушка,
что в `#pax-modal`: он устаревает, если тронуть состав уже после него.
`dropCompPreview()` на `input`/`change` гасит расчёт и возвращает кнопке
подпись «Рассчитать» — иначе применилось бы не то, что оператор видел.

**Правка брони**: сохраняет номер брони, пересчитывает дельту мест атомарно.

**Паспорт**: предупреждение если истекает до поездки или раньше чем через 6 мес после.

**Правила оплаты**: 30% в течение 3 дней с брони + 70% не позднее чем за 14 дней
до выезда. Если до выезда меньше 14 дней — 100% в течение суток. Считает
`TuronApi.paymentPolicy(dateStart)`, рисует `TuronCatalog.policyHtml(dateStart, total)` —
одна реализация на карточку тура и окно брони.

**14 дней — ОДНА граница на два правила**, `TuronApi.FINAL_DAYS`. До неё
действует рассрочка и отмена без удержания, после — платить надо всё сразу и
при отмене удерживается 100%. Разъехаться им нельзя: уедет платёж раньше
штрафа — между этими датами деньги уже собраны полностью, а отмена ещё
бесплатная, и оператор возвращает всё. Число было продублировано в `api.js`,
`app.js` и `catalog.js`; теперь оно одно и экспортируется.

Мало вынести само число — **считать дни надо одинаково**. Оплата брала
`new Date()` с текущим временем и `Math.floor`, отмена — полночь и
`Math.round`: в один и тот же день после полудня оплата видела 13 дней
(«платить всё сразу»), а отмена 14 («ещё бесплатно»). Граница зависела от
часа, когда агент открыл кабинет. Теперь обе идут через одну
`daysBetweenDates` — от ПОЛУНОЧИ дня отсчёта до полуночи дня выезда, —
а `daysUntilDeparture` её же обёртка. Проверено тестом на всех сроках
подряд, а не только на 14-м дне.

Решение оператора по границе: **14-й день ещё бесплатный**, штраф и
требование полной оплаты включаются с 13-го — вместе, одним переходом.

**Дата рождения проверяется на существование**, а не только на формат:
`invalidBirthDate` ловит 31 февраля (разбором обратно в строку), даты
раньше 1900 года, будущие и те, что позже выезда. Функция ЖИВЁТ В ДВУХ
МЕСТАХ — `worker/index.js` и `js/api.js` — намеренно: демо-режим
(`preview.html`, ui-тесты) идёт мимо воркера целиком, и без копии
«31 февраля» проходило бы в браузере молча. По дате считается тариф и
признак младенца, то есть цена и число мест. Правите одну копию — правьте
вторую.

**Переплата законна и не блокируется.** Клиент мог доплатить сверху,
курс мог сыграть — оператор такие случаи проводит. Но и молча она не
проходит: `POST /api/admin/payments` без `allow_overpay` возвращает
**409** с расчётом (`balance`, `amount`, `excess`), кабинет показывает
сумму превышения и спрашивает подтверждение, и только после него шлёт
повтор с `allow_overpay: true`. Демо-путь возвращает ту же ошибку с тем
же `err.data`, поэтому окно подтверждения проверяется тестом.

**Калькулятор заезда**: счётчики строятся из прайса самого заезда
(`departure_prices`), а не из захардкоженных возрастных групп — иначе при смене
тарифной сетки калькулятор считал бы не то, что сервер. Верхняя граница детского
тарифа не включается, поэтому `Chd 5-10` подписан как «5–9 лет».
Из калькулятора форма брони открывается на нужное число строк; взрослым
подставляется размещение, детям нет — их тариф определит дата рождения.

**ФИО вводится ТРЕМЯ полями — фамилия, имя, отчество**, а в базе остаётся
одна строка `full_name`. Склейка и разбор — `TuronApi.joinName` /
`TuronApi.splitName`, и живут они в `api.js`, а не в `app.js`, потому что
нужны обоим кабинетам: агентской форме брони и операторскому окну правки
документа. Разъехаться им нельзя — что один экран разобрал, другой обязан
собрать обратно так же.

Разбор идёт по первым двум пробелам: фамилия, имя, ОСТАЛЬНОЕ в отчество.
Двойные фамилии и составные имена в паспортах встречаются, и хвост терять
нельзя, поэтому остаток склеивается, а не отбрасывается.

Отчество не обязательно: в загранпаспортах его часто нет. Обязательны
фамилия и имя — проверка в `updateSummary` смотрит именно на них, а не на
непустой `full_name` (одна фамилия дала бы непустую строку и прошла бы).

Три поля лежат в своей подсетке `.tt-pax-name` на три колонки поверх общей
`.tt-pax-grid` в две: по общей сетке третье поле уехало бы на свою строку и
встало рядом с датой рождения, будто относится к ней.

**Строка формы брони помнит, какой тариф выбрал агент.** Конструктор и
калькулятор карточки кладут в строку `tariff` (`ADULT` или код детского
тарифа), форма пишет его на карточку в `data-tariff`, показывает подписью
рядом с «Пассажир N» и предупреждает, если дата рождения даёт ДРУГОЙ тариф.
Раньше детская строка уходила пустым объектом: размещение разворачивалось в
первый пункт списка, и «взрослый SNG + младенец DBL» выглядел как состав,
который не подхватился (жалоба агентства). Теперь ребёнку подставляется
размещение ВЗРОСЛЫХ — он и живёт в их номере, а на цену это не влияет:
детский тариф в `priceFor` берётся раньше размещения.

Тариф хранится на самой карточке, а не в поле ввода, поэтому
`collectPassengers` читает его из `card.dataset.tariff` — иначе он терялся
бы при каждой перерисовке (кнопки «Добавить» и «Убрать»).

**Конструктор «Новый тур»** (вкладка билдера, первый экран агентства): работает
на выбранном заезде — тарифы, суммы, комиссия и график платежей считаются из
`state.builder` и прайса заезда. Выдуманных пассажиров, рейсов и сумм там быть
не должно: агент называет клиенту то, что видит. Кнопка «Забронировать» открывает
ровно тот заезд и состав, что показаны. Рейсы и отели — из `js/provisional.js`.

Раскладка намеренно повторяет кабинет mir-jahon (агентства к нему привыкли):
период + отель сверху, «Авиабилеты» таблицей, слева счётчики туристов и правила
оплаты, по центру «С билетом / Без билетов» + питание + «Общий», справа фото,
ниже «Бронирование» и «Скачать программу», внизу три столбца
включено / доп. расходы / информация (грузятся через `TuronApi.catalogTour`).
Классы этого экрана — `tt-mj-*`.

**Заезды не фильтруются по полётной программе.** Раньше кабинет показывал
только даты из `isScheduledDate` (пятницы до 11.09.2026) — заезд на новый сезон
или не в пятницу молча исчезал бы из продажи, хотя в публичном каталоге он
виден. Теперь от расписания зависит только блок рейсов: нет рейса — пишем об
этом, но заезд продаётся.

**Предварительные данные**: `js/provisional.js` — единственное место для данных
вне БД. Часть уже подтверждена оператором (рейсы Centrum Air и расписание,
отели с ночами 4+3 и ссылками на booking.com, контакты, PDF-программы) и
плашками не помечается. Неподтверждённое (бланк ваучера) помечается
`.tt-provisional`. Выдуманных данных вне этого файла быть не должно.

**Доп. кровать (DBL+1) — двое взрослых + ребёнок на доп. кровати.** Нужна
как тип размещения, чтобы оформить это в брони и не иметь претензий от
туриста. Отдельной цены у неё НЕТ: ребёнок на доп. кровати считается по
обычному детскому тарифу по возрасту — той же `priceFor`, что и везде
(детский тариф берётся раньше размещения, поэтому код `DBLX` для ребёнка
не применяется). Предупреждения о «предварительной цене» нет —
уточнять нечего.

В прайсе у `DBLX` стоит цена `= TRPL` как запасной тариф на случай, если
доп. кровать займёт ВЗРОСЛЫЙ (крайний случай, не основной сценарий):
«третий в номере» — ближайший реальный тариф. Если оператор задаст
отдельную цену для взрослого на доп. кровати — блок `EXTRA_BED_*` в
`tools/build-seed.py`, пересборка сида, миграция по образцу
`007-extra-bed.sql`.

**Новый тип размещения не требует правок интерфейса.** Форма брони,
калькулятор заезда и билдер строят список размещений из `departure_prices`
самого заезда (`kind='placement'`), а сервер так же ищет тариф по коду —
захардкоженных SGL/DBL/TPL в коде нет. Достаточно строки в прайсе и подписи
в `PLACEMENT_LABELS` (`tools/build-seed.py`). Так и добавился `DBLX`.

**Верхняя панель кабинета постоянная** — одинаковая на всех вкладках: бренд,
курсы ЦБ, Telegram, колокольчик, агентство и «Выход». Название текущего
раздела — в отдельной полосе `.tt-workspace-subhead` под панелью (на билдере
скрыта, у него свой заголовок). Так просили агентства.

**Флаги языка — свой дропдаун, не `<select>`.** Нативный список пунктов
рисует ОС: на тёмной теме он был белым системным со светлым невидимым
текстом, и картинку-флаг в `<option>` вставить нельзя. Флаг-эмодзи не
годятся — Windows их не рисует, показывает буквы «RU». Поэтому виджет
`#lang-widget` (кнопка + `<ul>`-меню) с инлайновыми SVG-флагами в
`js/public-ui.js` (`FLAGS`, `initLangWidget`). Union Jack упрощён.

**Переключателя языка в кабинете нет намеренно.** Он остался только в публичной
шапке, где переводы реально есть (`js/public-ui.js`, 4 словаря). Кабинет — 374
строки русского текста в `app.js`/`admin.js`, поэтому кнопка в нём была бы
обманом: агент нажимает UZ и ничего не меняется. Появится кнопка — только
вместе с настоящим переводом кабинета.

**Уведомления (колокольчик)** считаются на клиенте из `state.bookings`
(`collectNotices` в `js/app.js`) — отдельного запроса нет. Четыре типа:
просроченный этап оплаты (по `paymentPolicy`), готовые документы
(`docsUnlocked`), выезд в ближайшие 7 дней, проблема с паспортом
(`TuronApi.passportIssue`). Незаполненный паспорт через интерфейс не
возникает — форма брони не даёт отправить без него, проверка оставлена
страховкой для данных из других источников. У оператора свой набор
(`loadOperatorNotices`): лента действий агентств и открытые заявки на
отмену, плюс те же просрочки и скорые выезды по всем броням.

**«Отметить прочитанным»**. Уведомления — факты, а не события с id: они
пересчитываются заново при каждой отрисовке и пропадают сами, когда
условие перестаёт выполняться (долг погашен, паспорт продлили). Поэтому
статус «прочитано» не хранится вместе с уведомлением — у него просто нет
устойчивой записи, которую можно было бы пометить. Вместо этого у каждого
уведомления есть **стабильный ключ** (`"late:" + code`, `"pass:" + code +
":" + индекс`, `"ev:" + id` у оператора и т.д.), а отметки — отдельный
список ключей в `localStorage`, свой на каждый аккаунт (`turon.notices.seen.
<id агентства>`). Ключ НЕ включает переменные величины вроде суммы долга:
просрочка по той же брони — то же самое уведомление, растущий долг не
должен дёргать колокольчик заново; а вот просрочка по ДРУГОЙ брони — уже
новый ключ, значит непрочитанная.

Счётчик на колокольчике — число **непрочитанных**, не общее число
уведомлений (оно отдельно показано в шапке панели). Кнопка «Отметить
прочитанным» видна, только пока есть непрочитанные, и сама пропадает
вместе со счётчиком. Прочитанные пункты не удаляются из списка — вопрос
мог остаться нерешённым, — а тускнеют (`.is-read`), чтобы взгляд отличал
новое от уже увиденного. Клик по кнопке не закрывает панель: агент должен
увидеть, как список гаснет, — это и есть подтверждение, что сработало.

**Курс ЦБ идёт через воркер**, а не напрямую из браузера: `cbu.uz` не отдаёт
CORS-заголовки, поэтому прямой запрос молча падал и в табло висела зашитая
устаревшая цифра. Теперь `GET /api/public/rates` тянет курс на сервере и кэширует
на час; в табло рисуется целыми числами по `[data-rate]` (их несколько — в
публичной шапке и в кабинете).

**Уведомление в Telegram о новой брони** (`notifyTelegram` в
`worker/index.js`): после успешной вставки брони воркер шлёт оператору
сообщение через `ctx.waitUntil` — фоном, чтобы клиент не ждал Telegram, а
его сбой не ронял уже сохранённую бронь. НЕ ДЕЛАЕТ НИЧЕГО, пока не заданы
`TELEGRAM_BOT_TOKEN` (секрет: `wrangler secret put`) и `TELEGRAM_CHAT_ID`
(в `wrangler.toml [vars]`). Комиссия в сообщение не идёт — бот может быть
в общей группе. Имена и агентство экранируются (`tgEscape`), иначе символ
`<` в названии ломал бы `parse_mode: HTML`.

**Отправку делает одна `tgSend(token, raw, text)`** — она же у заявки на
отмену и у формы «Свяжитесь с нами». `raw` может быть НЕСКОЛЬКИМИ id
через запятую (менеджеров двое).

Адресат сейчас один на всё — группа «Etihad — брони» (`-1004306422816`), и
бот тоже один: `CONTACT_TELEGRAM_BOT_TOKEN` хранит тот же токен, что и
`TELEGRAM_BOT_TOKEN`. Пара переменных осталась раздельной намеренно —
на случай, когда заявки с сайта и брони разойдутся по разным адресатам.

**Личные id в списке работают только после Start.** Бот физически не
может написать человеку первым: пока получатель не открыл бота и не
нажал Start, Telegram отвечает 403, а мы ошибку глушим — сообщения молча
не приходят. Поэтому прежние личные id из `CONTACT_TELEGRAM_CHAT_ID`
убраны в пользу группы: в группу бот пишет как участник, без Start от
каждого. Возвращать личный адрес — только вместе с нажатым Start.

**МИНА: id обычной группы меняется, когда она становится супергруппой**
(добавили третьего участника, включили темы, сделали публичной) — Telegram
не пересчитывает старый id, а выдаёт новый, начинающийся с `-100`, и все
сообщения в старый id молча проваливаются (`tgSend` глушит ошибку, а
`delivered: false` в ответе формы легко принять за «не настроено», а не
«переехало»). Первый `-5387995567` умер именно так, ровно после добавления
второго менеджера в группу.

Диагностика без похода в код — прямой вызов `sendMessage` тем же токеном
и chat_id в обход воркера: если группа мигрировала, Telegram сам вернёт
`"Bad Request: group chat was upgraded to a supergroup chat"` и поле
`migrate_to_chat_id` с новым id — его и подставлять. Симптом «форма
откатывается на mailto» / «бронь создаётся, а сообщения в Telegram нет»
почти всегда это, а не сломанный секрет.

Ошибки в `tgSend` глушатся намеренно: бронь уже сохранена, ронять её
из-за Telegram незачем — но это же значит, что смена id никогда не
покажет себя ошибкой в интерфейсе, только тишиной.

**Разделы агентства**: «Туристы», «Платежи» и «Документы» строятся на клиенте из
`/api/bookings` — отдельных маршрутов у них нет. «Платежи» считают сроки через
`TuronApi.paymentPolicy(date_start, created_at)` и подсвечивают просрочку.
«Документы» собирают три бланка на бронь — счёт, ваучер и авиабилет
(маршрут-квитанцию) — и открывают их на печать в новом окне.

**Документы выдаются по ходу сделки, а не все сразу.** Бланков три, и
порядок между ними жёсткий:

1. **Счёт на оплату** — доступен СРАЗУ после брони. По нему платит клиент,
   поэтому держать его за замком бессмысленно. Суммы, график платежей и
   остаток берутся из самой брони и `TuronApi.paymentPolicy` — той же,
   что рисует «Платежи» и окно брони. Своей арифметики в счёте нет
   намеренно: разойдись он с кабинетом — спорить будет не о чем.
2. **Ваучер** и **авиабилет** — только когда `docsUnlocked(b)`, то есть
   `balance <= 0.01` при неотменённой брони. Это документы на уже
   оплаченную услугу; выдать их по неоплаченной броне значит отдать
   клиенту подтверждение того, за что оператор денег не получил.

Порог — **полная** оплата, а не первый взнос: по 30% предоплаты билет ещё
не выписан, и подтверждать перелёт нечем.

Кнопки закрытых бланков **гасятся, а не прячутся** — агент должен видеть,
что он получит после оплаты, иначе раздел читается как поломанный. Рядом
строка `.tt-doc-state` объясняет остаток. `openVoucher`/`openTicket`
проверяют `docsUnlocked` ещё раз у себя: список мог устареть между
отрисовкой и кликом.

Момент открытия попадает в колокольчик (`kind: "docs"`, зелёная галочка,
ведёт на вкладку «Документы»). Оператор проводит оплату у себя, и без
этой строки агентство узнавало бы о готовности бланков, только случайно
заглянув в раздел.

Ловушка вёрстки: в «Документах» ТРИ кнопки, а базовая сетка
`.tt-row-card` рассчитана на четыре равные колонки — «Счёт на оплату»
переносился по слогам и вылезал за рамку. Поэтому у `#documents-list`
своя раскладка в две колонки.

**Бланки повторяют ваучер, который оператор уже выдаёт клиентам** —
генератор лежит в соседнем репозитории `etihad-voucher`. Оттуда взяты
раскладка и цветовые токены (лесной зелёный `#14432c`, латунь `#b6912f`,
слоновая кость `#f7f4ec`): шапка с номером заказа справа, карточки
пассажиров с иконкой, перелёт двумя плечами с кругом-самолётом между
городами, блок услуг справа, отели, подвал с контактами.

**Бренд — Etihad, и это НЕ авиакомпания.** Оператор работает под двумя
именами: Turon Tour и Etihad (второе юрлицо — умра, хадж, теперь и
авторские туры); выступают под вторым, оно узнаваемее. Совпадение с
Etihad Airways — только по имени: у авиакомпании знак это золотая
арабская вязь الاتحاد со своей угловатой гарнитурой, а здесь свой
зелёно-золотой изгиб с обычной антиквой. Знак лежит в
`img/etihad-logo.png` (локап со словом, для шапок бланков) и
`img/etihad-mark.png` (только изгиб, для шапки сайта, где слово рядом
текстом). Рейсы выполняет Centrum Air по прямому партнёрству — отсюда
и чартеры.

**QR-кода на билете нет намеренно** (просил оператор). Посадочный талон
выдаёт авиакомпания на регистрации, а этот бланк — подтверждение перелёта
в составе тура; сканировать его негде, и нарисованный «для вида» квадрат
создавал бы впечатление, что документ проходит по контролю.

Раскладка сверена с образцом построчно: знак по ЦЕНТРУ листа (три колонки
grid, не flex — иначе знак съезжал бы от длины номера заказа), номер заказа
справа без даты, подписи полей С ДВОЕТОЧИЯМИ, рамки карточек ЗЕЛЁНЫЕ
(бежевые на белом листе не читались), имена и названия отелей капсом.

Плечи перелёта — ДВЕ РАЗДЕЛЬНЫЕ карточки с зазором, самолёт висит над
зазором и перекрывает обе (`position:absolute` + белый `box-shadow`-ободок).
Сначала я срастил им борта в одну рамку — в бланке оператора не так, там у
каждого города своя замкнутая рамка.

Реквизиты внизу идут СТРОКОЙ «АГЕНТСТВО: B2 MANAGEMENT…», подпись и
значение на одной линии, а не подписью над значением. Адрес мельче
остальных (`.req-sm`), под блоком тонкая линия.

Отели идут БЕЗ своего заголовка, сразу за маршрутом — отдельная
«Проживание» разрывала блок надвое. Внизу реквизиты карточками
(агентство, телефон, почта, адрес), а не подвалом мелким шрифтом.

Трансферы и экскурсии собирает `TuronProvisional.servicesFor` ИЗ МАРШРУТА,
а не из захардкоженного списка: маршрут зеркальный, и строка
«Батуми → Ризе» на заезде через Трабзон отправляла бы группу не в ту
сторону. Города берутся из `HOTELS` того же transport, поэтому порядок
всегда совпадает с фактическим расселением.

Стили бланков инлайном в `voucherCss()`/`ticketCss()`: окно открывается
через `document.write`, `styles.css` туда не подключён. Логотип — по
АБСОЛЮТНОМУ адресу: окно пустое (`about:blank`), относительный путь в нём
не разрешается.

**Ловушка:** `brandLogoUrl()` и `ticketCss()` объявлены между `voucherCss()`
и `PLANE_SVG`. Правя `voucherCss()` целым блоком, их дважды сносило —
бланк падал с `is not defined`. Меняйте функцию по месту, а не заменой
диапазона.
«Сообщения» — переписки нет, показаны контакты оператора.

**Публичный каталог**: гость видит направления, туры, программу и цены без
входа — агент показывает тур клиенту по ссылке (`#/t/KARADENIZ`). Комиссии в
публичные ответы не попадают вообще. Бронь только после логина: гость жмёт
«Войти и забронировать», заезд запоминается и окно брони открывается сразу
после входа. Гостю остаток мест показывается ведром («20+ мест»), агенту в
кабинете — точным числом.

**Титульный экран каталога** (`.tt-public-intro`, рисуется в `js/catalog.js`) —
видео `img/hero-travel.mp4` на весь блок, текст и панель поиска колонкой по
центру, раскладка повторяет asialuxe. Центральная, а НЕ левая как раньше:
в ролике композиция центрированная (глобус ровно посередине), и текст слева
наезжал бы прямо на него. Затемняем верх и низ под надписи, середину
оставляем открытой; под самим текстом — мягкая радиальная подложка
(`.tt-public-hero-copy`), иначе подпись не читается на пёстрых иконках.
Прежняя карусель из трёх фотографий удалена вместе с `slider.*`-строками и
`initHeroCarousel`.

**Герой и «Ближайшие заезды» — один холст с одним роликом.** Обёртка
`.tt-hero-canvas` (в `js/catalog.js`) содержит `.tt-hero-bg` с видео,
`.tt-public-intro` и `.tt-upcoming`. Фон-слой **липкий** (`position: sticky;
height: 100svh`) и остаётся на экране, пока холст прокручивается мимо, а
контент вытянут поверх него `margin-top: -100svh`. Поэтому переход между
первым и вторым экраном бесшовный ПО ПОСТРОЕНИЮ: склеивать нечего, фон
буквально не меняется, второй ролик не грузится.

Растягивать видео на оба экрана нельзя: `cover` центрирует кадр по блоку,
и глобус уехал бы на середину холста — то есть в заезды, а из героя пропал.
Высота 145svh переехала с секции на сам `.tt-hero-video`
(`top: calc((100svh - 145svh) / 2)`) — геометрия обрезки та же, что была
у блока, кольцо целое вплоть до 21:9, но кадр стоит в окне неподвижно.

Две мины, на которых уже подорвались:
- `:root:not([data-theme="light"]) #screen-public .tt-public-intro` задаёт
  СПЛОШНОЙ `background: #05090b` и по специфичности (0,3,1) бьёт
  `.tt-hero-canvas > .tt-public-intro` (0,2,0). Пока его не погасили в том
  же скоупе, чёрная плита закрывала видео на первом экране и на стыке с
  заездами шла резкая линия. Тонировку холста тоже задаём на
  `.tt-hero-bg::after`, а собственные `::before/::after` героя внутри холста
  гасятся `content: none` — иначе двойное затемнение и градиент посреди блока.
- `.tt-hero-canvas` надо было добавить в СПИСОК ИСКЛЮЧЕНИЙ полноширинности
  (`#screen-public > .tt-container > :not(...)`), причём в ОБЕИХ копиях —
  базовой и внутри мобильной медиазапросы.

**У блока заездов нет заголовка** — карточки идут сразу под панелью
поиска (так просили). Название осталось только в `aria-label` секции:
без него секция была бы безымянной и на слух читалась как «просто набор
кнопок с датами». Стрелки слайдера поэтому лежат ПОВЕРХ краёв дорожки,
а не строкой над ней — отдельная полоса под них вернула бы тот самый
отступ, ради снятия которого заголовок и убрали. Ключи `upcoming.kicker`
и `upcoming.text` удалены из всех четырёх словарей.

**Карточки заездов — фото направления плюс матовое стекло.** Кадр берётся из
`DESTINATION_PHOTOS` по **значению `tours.destination`** (`«Турция»`), а НЕ по
заголовку плитки (`«Турция и Грузия»` живёт в `destinations.title` и сюда не
приходит) и НЕ по номеру карточки. Прежний `ROUTE_PHOTOS[i % длина]` был общим
списком на всё: заезд умры выходил с видом Батуми, а два заезда одного тура
получали разные кадры. Направления без своих фотографий (умра) карточку не
теряют — рисуются без фото, стекло работает поверх заливки. Промах по ключу
молча оставляет ВСЕ карточки без фото, поэтому тест проверяет не формат пути,
а что хотя бы один кадр реально подставлен.

Слайдер (`initUpcomingSlider`) листает дорожку `scroll-snap-type: x proximity`.
Это НЕ тот постраничный снап, что снимали со страницы: он заперт внутри дорожки
и вертикальную прокрутку не трогает. Дорожка на **flex**, а не grid — у grid
колонки задавались через `minmax(0, …)` и 12 карточек послушно ужимались до
80px вместо переполнения, прокручивать было нечего. Шаг стрелок меряется по
факту (ширина карточки + gap), а не задан числом, иначе врал бы на каждом
брейкпоинте.

**Карточки без фото несут водяной знак Etihad, а не пустуют.** У умры
(и любого направления без своих кадров — фотографий туров нет вообще, TODO)
глазу не за что зацепиться: стеклу нечего размывать без фотографии позади,
и панель у нижнего края смотрелась голым текстом на плоской заливке.
`.tt-up-card.is-plain::before` кладёт `img/etihad-mark.png` бледным клеймом
в пустующий верх, `.tt-up-glass` для таких карточек — с более плотным
собственным фоном (`rgba(6,22,18,.58)`), не полагающимся на backdrop-blur.

**Сетевые сбои переживаются, а не показываются.** Симптом был «иногда
захожу на сайт — не удалось загрузить каталог», и причин оказалось две.

1. `request()` в `js/api.js` делал ровно один `fetch` без таймаута. Воркер
   после простоя стартует на холодную, D1 отвечает не всегда с первой
   попытки, телефон на ходу переключается с Wi-Fi на LTE — любой такой
   случай отклонял запрос, и каталог насмерть заменялся текстом ошибки.
   Теперь переходные сбои (обрыв сети, таймаут 12 с, 5xx, 429)
   перезапрашиваются с паузами 400 и 1200 мс.

   **Повторяется ТОЛЬКО `GET`.** Повтор `POST /api/bookings` создал бы
   вторую бронь на тех же пассажиров — это хуже любой ошибки загрузки.
   4xx тоже не повторяются: 401/404/400 детерминированы, повтор вернёт
   то же самое. Оба правила закреплены тестами.

2. Гонка при быстрой навигации: пока ответ ехал, пользователь уходил
   дальше, и опоздавший запрос завершался поверх нового экрана — сорвался
   он (ошибка затирала нормально открытую страницу) или доехал успешно
   (возвращался экран, с которого уже ушли). В `js/catalog.js` каждая
   отрисовка забирает номер `drawSeq`, и опоздавшие ответы отбрасываются
   (`stale`/`failWith`). **Добавляя новый `render*`, не забудьте про пару
   `nextSeq()` в начале и `failWith(seq)` вместо голого `errorBox`** —
   иначе гонка вернётся именно в этом разделе.

У экрана ошибки теперь есть кнопка «Повторить» (`[data-catalog-retry]`):
раньше единственным выходом была перезагрузка страницы.

**Адрес — единственный источник правды для экранов.** Один хеш-роутер в
`js/app.js`: `#/` каталог, `#/d/X` направление, `#/t/Y` карточка тура,
`#/login` вход, `#/app/<вкладка>` кабинет. Клик, «назад»/«вперёд» и F5
проходят через один `applyRoute`, поэтому расходиться им негде: обновление
на `#/app/payments` возвращает на «Платежи», а не на первую вкладку.
Кабинет по ссылке без сессии уводит на вход и после него открывает именно
то, что просили (`pendingRoute`). Логотип кабинета ведёт на главную,
в публичной шапке вошедшему показывается «Кабинет» и возвращает на
последнюю вкладку (`lastAppRoute`).

Редиректы сделаны **циклом со счётчиком**, а не рекурсией: `resolveRoute`
возвращает адрес для переезда либо `null`. На рекурсии уже подорвались —
`tabAllowed` проверялся до `ensureCabinet`, который и выставляет флаги
вкладок по роли, поэтому у свежей операторской сессии `manifest` числился
скрытым и редирект уходил сам на себя. Вкладку, недоступную роли, правит
`showApp` через `replaceState`, а НЕ через `navigate` — иначе снова круг.

**Смена экрана — плавным проявлением**, анимируется только прозрачность.
`transform` тут нельзя: он создаёт containing block, и `position:fixed`
внутри (окно брони, затемнение меню) начнёт считаться от экрана, а не от
окна.

**Кнопка день/ночь** (`#theme-toggle` в публичной шапке). Тёмная — по
умолчанию, светлая — прежнее кремовое оформление: тёмные правила навешены
через `:root:not([data-theme="light"])`, поэтому «день» просто перестаёт их
применять. Выбор в `localStorage`, начальное значение ставит инлайн-скрипт
в `<head>` — до загрузки `styles.css`, иначе мелькает белая вспышка. Тот же
скрипт продублирован в `tools/build-preview.js`: превью собирает свой
`<head>`, и без него `preview.html` открывался вообще без `data-theme`.

**У кабинета своя тема** (`#app-theme-toggle` в шапке кабинета), ОТДЕЛЬНАЯ
от публичной: атрибут `data-app-theme` на `<html>`, ключ `turon.app-theme`,
по умолчанию кремовая (агентства привыкли) — тёмная только по клику.
Начальное значение ставит тот же инлайн-скрипт в `<head>` (и в
build-preview.js), иначе при выбранной тёмной мелькнул бы кремовый кадр.

Тёмная палитра кабинета скоупнута на `:root[data-app-theme="dark"]
#screen-app` — публичную часть не трогает. Ловушки, на которых уже
поймались:
- `#screen-app` переопределяет только ПЕРЕМЕННЫЕ, а `body` держит тёмный
  `color: var(--tt-text)` из базовой темы (он вне скоупа). Текст без явного
  цвета (`<strong>` в карточках) наследовал бы тёмное на тёмном — поэтому в
  скоупе задан и сам `color: var(--tt-text)`, не только переменная.
- `--tt-forest` был тёмным ТЕКСТОМ → в тёмной делаем светлым. Но им же были
  крашены ФОНЫ (сайдбар через `--tt-forest-deep`, зелёные плашки, аватар) —
  их перевели на `--tt-forest-soft`, который остаётся тёмным, иначе фон
  засветился бы вместе с текстом.
- Плашки шапки (курс, TG, колокольчик, аккаунт) — общий кремовый «жетон» с
  белым внутренним кантом (`::before`) и inset-бликом; в тёмной гасятся
  отдельно, одной сменой `background` не обойтись.

**Секция «Свяжитесь с нами» — только форма, по центру.** Контактов в ней
НЕТ намеренно: раньше слева стояла карточка с телефоном, почтой и адресом —
теми же самыми, что в подвале двумя блоками ниже. Посетитель дочитывал
«соберём тур под вас» и упирался в выбор: писать, звонить или заполнять
форму. Осталось одно действие; контакты никуда не делись, они в подвале.

Текст «мы не ограничены готовыми программами» переехал из убранной карточки
под заголовок — это и есть смысл блока, терять его вместе с карточкой
нельзя. Кнопка простая (однотонная заливка, радиус 6px): прежний бирюзовый
градиент с тенью в 38px читался как заглушка, а не как отправка формы.

Фотографии в секции НЕТ — пробовали крыло над облаками на закате
(`img/contact-sky.jpg`), оператор её отклонил, файл удалён. Фон — три
градиентных слоя, как и было до неё. **Фотографии в PNG на публичные
страницы не класть** — для фотографии это всегда мегабайты вместо десятков
килобайт (тот снимок пришёл PNG-шкой на 1,6 МБ и ужался `ffmpeg -q:v 6`
в 88 КБ). Сборка превью, кстати, ловит ссылку на несуществующий файл и
падает — стили с картинкой нельзя закоммитить раньше самой картинки.

**Поля формы подписаны СВЕРХУ, а не placeholder'ом** (`cfField` в
`js/catalog.js`, классы `.tt-cf-field` / `.tt-cf-label`). Серый текст
внутри рамки читается хуже белого над ней и пропадает, стоит начать
печатать: заполнив три поля, человек уже не видит, где что. Placeholder
остался только у сообщения — там он приглашение («Расскажите о
поездке…»), а не название поля; название пришло новым ключом
`contact.messageLabel` во все четыре словаря.

Класс `.tt-cf-*`, а не общий `.tt-field`: тот занят формами кабинета и
лежит в `styles.css` выше — правило оттуда перебивало бы это. Та же мина,
что была с `.tt-contact-grid`.

**Блок идёт во всю ширину, но текст в нём — в ДВЕ колонки:** крупный
заголовок (до 3.8rem) слева, абзац справа, посаженный на нижнюю линию
заголовка. Пустить абзац во всю ширину нельзя — строка в 1180px это под
двести знаков, и глаз, дочитав до конца, не находит начало следующей.
Две колонки дают блоку полную ширину, а строке — читаемую длину. Ниже
900px колонки складываются в одну.

Выравнивание по ЛЕВОМУ краю, а не по центру: заголовок, абзац и форма
начинаются с одной вертикали, и разница в их ширине перестаёт читаться
как расхождение. Заодно у абзаца ровный левый край — центрированный
рвётся по обеим сторонам.

**Поля формы — в два столбца** («Имя» и «Телефон» рядом, сообщение и
кнопка через оба): растянутое на 1100px поле под имя выглядит как поле
под адрес. Ниже 720px столбец один.

**Кнопка НЕ во всю ширину** — полоса бирюзы в полтора метра кричит громче
всего блока и читается как баннер, а не как отправка формы. На телефоне
наоборот тянется: там ширина одна на всё.

**Мина, на которой поймались:** `.tt-contact-grid` был занят ДВАЖДЫ — этой
секцией и вкладкой «Контакты» в кабинете. Правило кабинета лежит в
`styles.css` НИЖЕ и молча перебивало публичное, поэтому секция не
слушалась собственной ширины. Кабинетный блок переименован в
`.tt-op-contact-grid`.

**Контакты в подвале** публичной страницы берутся из `js/provisional.js`
(`OPERATOR`), а не пишутся в разметке: телефон иначе пришлось бы править
в двух местах. Раскладка подвала от темы не зависит — на этом уже
поймались, базовое правило делает его флексом в капсе.

**Менеджеров несколько** (`OPERATOR.managers`): Аскар и Ойбек. Вкладка
«Контакты» в кабинете рисует весь список, а подвал и ваучер читают плоские
поля `OPERATOR.phone`/`contact_name` — там место ровно под один контакт.
Плоские поля берутся из `managers[0]`, чтобы телефон не жил в двух местах.
Telegram есть у обоих — оператор подтвердил, что у Ойбека он на том же
номере, поэтому ссылка собрана как `t.me/+номер`.

**Публичная часть тёмная, кабинет светлый.** Палитра гостевых экранов
(почти чёрный фон, бирюзовый акцент `#4fd6e8`) задана переопределением
`--tt-*` на `#screen-public`, а не на `:root` — кабинет остаётся кремовым,
агентства к нему привыкли и перекрашивать его не просили. Почти всё внутри
уже ходит через переменные, поэтому руками правятся только места, где цвет
был зашит. Отдельно: у `#screen-public` ниже по файлу свой фон (кремовый
градиент с золотым отсветом), он объявлен позже — перебивается
специфичностью `body #screen-public`, а не порядком строк.

**Блок преимуществ** (`.tt-benefits`, четыре иконки под героем) — пересказ
строк `tour_content` c `kind='included'` тура Карадениз: перелёт, отели,
гид, поддержка. Писать туда то, чего нет в программе, нельзя — агент
пообещает клиенту услугу, которой не существует. Ссылок «подробнее» с
референса нет намеренно: тур пока один, и все четыре вели бы в одну карточку.
Колонок четыре только шире 860px, дальше две, а с 520px — одна: на телефоне
четырёхколоночная сетка ужимала колонку до одного слова, и заголовки
переносились по буквам за пределы карточки.

**Низ героя растворяется в фоне страницы** (`.tt-public-intro::after`), а не
обрывается кромкой. Тёмное видео, встык поставленное к кремовому фону светлой
темы, давало ровную горизонтальную линию — она читалась как дефект вёрстки.
Цвет градиента берётся из `--tt-bg`, а он переопределён на `#screen-public`
для тёмной темы, поэтому правило одно на обе. Плотность собрана у самой
кромки (`color-mix`-стопы на 58% и 84%), а не растянута линейно: ровный
переход тёмного в кремовый идёт через серый и выглядит мутной дымкой
в пол-экрана.

**Поиск на титульной** (`#tour-search`) ищет по РЕАЛЬНЫМ заездам
`GET /api/public/departures`, а не по выдуманному списку курортов: варианты
в выпадающих списках строятся из того, что действительно в продаже, поэтому
пустых обещаний в выдаче не бывает. Отдельный маршрут нужен именно потому,
что `catalogTours` отдаёт только `MIN(date)` по туру — фильтр «сентябрь» по
нему врал бы, выкидывая тур с ближайшим августовским заездом. Выдача
рисуется своей `searchResultRow`, а не `departureRow`: у поиска на руках
только сводка (`min_price`, остаток), полный прайс и расчёт — в карточке
тура, куда и ведёт кнопка. 404 от ещё не задеплоенного воркера гасится в
пустой список: поиск сузится до направления, страница не сломается.

**Экран входа** (`#screen-login`) — видео `img/login-flight.mp4` на весь экран,
одна стеклянная карточка формы по центру (`.tt-login-card`). Обсуждали три
раскладки (стекло по центру / панель слева + открытое видео / нижний блок),
выбрали первую: она одинаково хорошо работает и на десктопе, и на телефоне
без отдельной мобильной раскладки — просто `display:grid; place-items:center`.
Затемнение только мягкой виньеткой по краям (`::before`), НЕ плотным
градиентом через весь экран — иначе видео превращается в еле заметную
подложку. Видео сжато ffmpeg-ом (`scale=1280:-2,fps=24, crf 31, без звука`) —
было 13,3 МБ, стало 2,4 МБ; при замене исходника пересжимать так же,
не заливать сырой файл с телефона.

**МИНА, на которой поймались: интерфейс считал, что продуктов ровно ТРИ.**
Витрина «Новый тур» рисовалась из захардкоженного `BUILDER_PRODUCTS`
(Карадениз, Умра, Япония), операторская подпись заезда звала «Караденизом»
всё, что не умра, а фильтр направлений в разметке имел ровно два пункта.
Пока туры заводились только seed-файлом, это работало. С появлением вкладки
«Туры» заезд нового тура: у агентства не появлялся ВООБЩЕ (виден был только
на публичной титульной), у оператора подписывался чужим названием и не
фильтровался.

Теперь витрина — это три фирменные карточки ПЛЮС всё остальное из
`state.departures` (`builderExtraProducts`), подпись берёт `d.tour_name`,
а список направлений собирает `fillDirectionFilter()` из данных.
Закреплено тестом, проверенным на сломанном коде.

**Клик по такой карточке раньше не делал НИЧЕГО:** `openBuilderProduct` не
находил ключ среди трёх и выходил голым `return` — ни перехода, ни ошибки
в консоли. Неизвестный ключ теперь уходит в `openBuilderDestination`.

**Заголовки ведомости на узбекском — так и задумано** (`MANIFEST_COLUMNS`):
они повторяют настоящую ведомость оператора в Excel, чтобы менеджер открыл
файл и увидел привычную таблицу. Это не забытый перевод.

## Фотографии, которые загружает оператор

**Хранилищ поддерживается ДВА, воркер берёт то, которое подключено:**

- **R2** — правильный инструмент, он для файлов и сделан. Требует РАЗОВОГО
  включения в панели Cloudflare (dash → R2 → Enable): без этого
  `wrangler r2 bucket create` отвечает «Please enable R2 through the
  Cloudflare Dashboard [code: 10042]».
- **KV** — запасной путь, включать ничего не нужно, входит в Workers.
  Значения до 25 МБ, нам нужны сотни килобайт. Хранить картинки в KV — не
  то, для чего он задуман, но для нескольких десятков снимков это работает
  и не требует включать платный продукт.

Смысл развилки: оператор не должен ждать активации платного продукта, чтобы
поставить фотографию тура.

**МИНА: бинд на несуществующий бакет роняет `wrangler deploy` ЦЕЛИКОМ** —
не выкатывается ничего, включая правки, к фотографиям не относящиеся. На
этом уже подорвались: `[[r2_buckets]]` был вписан до создания бакета, и
деплой падал с 10042, хотя ломать было нечего. Поэтому оба блока в
`wrangler.toml` закомментированы, пока хранилище не заведено.

Пока хранилища нет, загрузка отвечает понятной ошибкой (503), а всё
остальное работает как прежде — кабинет не должен падать оттого, что
хранилище ещё не завели.

**Раздача идёт ЧЕРЕЗ ВОРКЕР** (`GET /api/media/<ключ>`), а не публичным
адресом бакета: не нужен ни отдельный домен, ни настройка публичного
доступа, а Cloudflare всё равно кэширует ответ на краю. Ключ уникален на
каждую загрузку, поэтому `Cache-Control` вечный — новый файл получает новый
ключ, старый адрес никогда не меняет содержимое.

**Обработки картинок на сервере НЕТ и не будет.** Снимок ужимается В
БРАУЗЕРЕ до отправки (`shrinkImage` в `js/admin.js`): 1600px по длинной
стороне, JPEG 0.82. С телефона приходят кадры на 8-12 МБ, а на странице они
показываются шириной от силы в тысячу точек — так не улетает трафик
оператора, воркеру не нужна библиотека (в Workers её и не поставить без
платного Images), а в хранилище не копятся оригиналы. Потолок 3 МБ на
сервере — страховка от загрузки в обход кабинета, а не рабочее ограничение.

**PNG и WebP пересобираются в JPEG** намеренно: прозрачность фотографиям не
нужна, а PNG для снимка — всегда мегабайты вместо десятков килобайт. Под
кадр подкладывается белый фон: JPEG прозрачность не хранит, и без подложки
прозрачные места стали бы чёрными.

**Файл уходит СЫРЫМИ БАЙТАМИ**, а не в JSON: base64 раздувает тело на
треть. Поэтому в `js/api.js` у `uploadMedia` свой `fetch`, а не общий
`request()` — тот всегда ставит `Content-Type: application/json`.
**Повторов при сбое нет намеренно**: повтор загрузил бы вторую копию файла
в хранилище, а пользы никакой.

**В демо-режиме хранилища нет** — `uploadMedia` возвращает сам файл строкой
`data:`. Так превью и ui-тесты проверяют весь путь «выбрал → ужалось →
подставилось в форму», не поднимая бэкенд.

**Обе ветки хранилища проверяются `test/media-test.mjs`** — воркер
импортируется обычным ES-модулем, а R2 и KV подменяются поддельными. Ветка
KV живёт ТОЛЬКО в воркере: демо-режим идёт мимо него целиком, и сломайся
она — все дымовые тесты остались бы зелёными.

**Ловушка самого теста:** сначала он проверял только JPEG и намеренно
сломанную KV-ветку НЕ поймал — у `serveMedia` запасное значение типа как
раз `image/jpeg`, и пропажа метаданных прошла незамеченной. Проверять
обязательно PNG: на нём запасное значение даёт неверный тип, и браузер
предложил бы скачать картинку вместо показа.

**Заглавный кадр карточки тура** — `tours.hero_image` (миграция
`018-media.sql`, НЕ идемпотентна). Раньше фон карточки был зашит в коде под
каждый продукт: видео у Карадениза, статичный кадр у умры, у нового тура
ничего. Загруженный кадр идёт ПЕРВЫМ и перекрывает встроенное оформление;
у тура без фотографии фона по-прежнему нет — это лучше пустой рамки.

Значение виджета лежит в СКРЫТОМ ПОЛЕ, а форма собирает его как обычный
ввод и про загрузку ничего не знает. Обработчики навешены на `document`, а
не на сами виджеты: формы кабинета перерисовываются целиком, и обработчики
терялись бы вместе с разметкой.

**Плитки направлений правит оператор** — свёрнутый блок «Плитки направлений»
во вкладке «Туры», `GET /api/admin/destinations` и
`POST /api/admin/destinations/:name`. Заголовок, подзаголовок, фотография,
порядок.

**Направление НЕ заводится и НЕ удаляется отдельно.** Оно появляется само,
как только у тура в поле «направление» написано новое слово; строка в
`destinations` — это только ОФОРМЛЕНИЕ плитки, и её может не быть вовсе
(тогда плитка рисуется по названию). Поэтому список собирается ИЗ ТУРОВ, а
не из таблицы `destinations`: направление без оформления в списке видно и
помечено «Без оформления», а оформление, оставшееся от направления без
туров, наружу не лезет.

**Имя направления здесь не меняется.** Оно связывает плитку с турами по
тексту, и переименование тихо отцепило бы оформление от всех туров сразу.
Название меняется в самом туре — тогда и плитка переедет вместе с ним.
Сервер отдельно отбивает попытку оформить направление, у которого нет
туров (404).

Запись идёт одним `INSERT … ON CONFLICT(name) DO UPDATE`: строки может не
быть вовсе, и разделять на INSERT/UPDATE незачем — ключ здесь имя.

Отдельной вкладки в меню оператора не завели намеренно: направлений мало и
правят их редко, а пустая вкладка занимала бы место постоянно.

## Языки (uz / en / tr / ru)

**Переведена ПУБЛИЧНАЯ часть, кабинет — нет и пока не планируется.** Кабинет
видят агентства-партнёры, которые работают на русском; публичную часть видит
клиент агентства — агент отправляет ему ссылку на тур. В кабинете 678
уникальных русских строк против 172 ключей словаря, и кнопка ради кнопки
показала бы полурусский экран.

**Словарь — `js/public-ui.js`, четыре языка, паритет ключей обязателен.**
Пропущенный ключ не падает ошибкой: на его месте рисуется само имя ключа
(`cat.book`). Проверять после каждой правки — сравнением наборов ключей,
а не глазами.

**Склонение по числу — `noun(n, основа)` и `counted(n, основа)`** в
`js/catalog.js`, формы берутся из `word.<основа>.one/few/many`. Русское
правило склонения применяется ТОЛЬКО к русскому: английскому оно врёт на 21
и 31 («21 departure»), а узбекскому и турецкому склонение после числа не
нужно вовсе — там во всех трёх формах одно слово.

**Порядок слов задаётся шаблоном, а не склейкой** — `fmt(шаблон, значения)`.
«младше 5 лет» по-узбекски это «5 yoshgacha»: приставка стала суффиксом, и
склейкой `"младше " + возраст` такое не собрать ни при каких условиях.

**Даты и разделитель тысяч идут за языком**, а не за зашитым `ru-RU`: иначе
на английской странице заезд подписан «31 июля», а цена «$1 200».

**МИНА: разбор русского текста регулярками.** На переводе взорвались бы три
места, и ни одно не дало бы ошибки в консоли — просто перестало бы работать:
- фильтр пакетов умры 13/10 читал первые символы строки «13 дней / 12 ночей»;
- цена пакета срезала приставку `/^от\s+/`;
- подпись перелёта срезала `/^Авиа · /`.
Все три переведены в данные. **Если пишете разбор строки, которая может быть
переведена, — вы пишете мину.**

### Контент из базы

**Переводы лежат в колонке `i18n` одним JSON** — `tours`, `destinations`,
`tour_variants`, `tour_content`. Формат: `{"uz":{"поле":…},"en":{…},"tr":{…}}`,
язык верхним ключом. Русский остаётся в обычных колонках и служит ОТКАТОМ.

Одна колонка, а не `name_uz`/`name_en`/`name_tr` по колонке на язык: пятый
язык или новое переводимое поле иначе означали бы миграцию И правку каждого
INSERT в воркере.

**Язык разрешает СЕРВЕР** (`?lang=en` → `localize()` в `worker/index.js`), а
не клиент. Публичный ответ уходит уже переведённым, и форма его не меняется:
`included` остаётся массивом строк. Иначе переводы пришлось бы протаскивать
через каждое поле в трёх файлах клиента, и любое забытое место молча
показывало бы русский там, где перевод есть. Цена решения — смена языка
требует перезапроса; она его и так делает (`turon:language` перерисовывает
каталог целиком).

**Русский `?lang=` не шлёт** (`langQuery` в `js/api.js`): он и так лежит в
обычных колонках, а лишний параметр мешал бы кэшированию.

**Админские маршруты `localize` НЕ вызывают** — редактору нужен сырой `i18n`,
иначе оператор не сможет править переводы. `tourContent` отдаёт его
разобранным объектом, публичные маршруты колонку наружу не отдают вовсе.

**Пустой перевод — это НЕ перевод.** `cleanI18n` не сохраняет язык, в котором
не осталось ни одного заполненного поля, а `localize` откатывается на русский
на пустой строке. Без этого в базе копились бы `{"tr":{}}` от каждого
сохранения, и карточка на турецком выходила бы с пустыми блоками вместо
русского текста. Закреплено тестами.

`cleanI18n` живёт В ДВУХ МЕСТАХ — `worker/index.js` и `js/api.js` — по той же
причине, что `invalidBirthDate`: демо-режим идёт мимо воркера целиком.
Правите одну копию — правьте вторую.

**Переводы правит оператор** — свёрнутый блок «Переводы» в каждой строке
редактора карточки тура. Свёрнут намеренно: тур заводят по-русски, а переводы
дописывают потом; развёрнутые поля втрое удлинили бы форму. Галочка в
заголовке показывает, что перевод уже есть, не разворачивая.

**У нового тура переводов нет вовсе** — это рабочее состояние, а не ошибка:
карточка выходит по-русски на любом языке, пока переводы не напишут.

Миграция — `db/migrations/016-i18n.sql`. **НЕ идемпотентна**: в SQLite нет
`ADD COLUMN IF NOT EXISTS`, повторный прогон падает с «duplicate column
name». Отказ безопасный, данные не меняются, но прогонять один раз.

### Переводы наполнения

**Лежат в `tools/seed-i18n.py`, ключ — ТОЧНАЯ русская строка** из
`build-seed.py`. Отдельным модулем, потому что в генераторе они утопили бы
сами данные; привязка при этом жёсткая — изменил русскую строку, и перевод по
ней больше не находится, в базу уходит NULL, карточка показывает русский.

**`tools/check-i18n.py` ловит две вещи** и вызывается из `test/run.sh`:
- **непереведённую строку** — предупреждение: у нового тура переводов нет
  вовсе, и это рабочее состояние;
- **осиротевший ключ** — ошибка: русский текст переписали, ключ нет, строка
  молча перестала переводиться. Симптом «я же переводил, а на сайте русский»
  почти всегда это. Проверено намеренной поломкой — ловится.

Строки, которые переводить нечего (код авиакомпании, коды аэропортов, время
вылета), перечислены явно в `NO_TRANSLATION`. Иначе проверка вечно
показывала бы «90 из 92», и настоящий пропуск на этом фоне не заметить.

**Описания и названия туров Умры собираются ПО ЧАСТЯМ** (`tour_i18n` в
`build-seed.py`), а не лежат строками в словаре: описание генерируется из
маршрута и списка отелей, и перевод целиком означал бы вторую копию тех же
отелей внутри описания. Две копии одного текста рано или поздно разойдутся.
Если перевода части нет — вся строка остаётся русской, полуфабрикат не
собирается.

**МИНА: `tours.destination` переводить нельзя.** По нему идёт группировка
плиток, фильтр `?destination=` и ветвление на клиенте (`destination ===
"Умра"`) — переведи его, и «Умра» на английском перестала бы находить свою
страницу. Ключ остаётся русским всегда, а заголовок страницы направления
приходит отдельным полем `destination_title` (переводится через `i18n`
таблицы `destinations`).

**Для ЖИВОЙ базы переводы приезжают миграцией `017-i18n-content.sql`**, а не
через `db/seed.sql` — тот стирает агентства и заезды. Собирается
`tools/build-i18n-migration.py`, руками не править.

Строки в ней ищутся **по русскому тексту, а не по id**: id в боевой базе свои,
выданные AUTOINCREMENT при её наполнении, и совпасть с локальными им неоткуда.
Отсюда ограничение: если оператор уже переписал русский текст строки через
редактор, перевод по ней не найдётся и строка останется русской — это лучше,
чем подставить перевод к другому тексту.

Идемпотентна: только UPDATE, ничего не создаёт и не удаляет.

Проверена на копии боевой схемы (без колонок i18n): 016 добавляет колонки,
017 проставляет 113 переводов, повторный прогон ничего не ломает. Без перевода
остаются ровно две строки — расписания Centrum Air, в которых переводить нечего.

## API-маршруты (worker/index.js)

```
-- без входа (публичный каталог):
GET  /api/media/:key                    — фотография из хранилища (вечный кэш)
GET  /api/public/rates                  — курс USD/EUR ЦБ РУз (кэш 1 час, обход CORS)
GET  /api/public/destinations[?lang=]   — направления с числом туров/заездов и «от $»
GET  /api/public/tours[?destination=][&lang=] — туры, опционально по направлению
GET  /api/public/tours/:code[?lang=]    — карточка тура: контент + предстоящие заезды
GET  /api/public/departures             — плоский список заездов для поиска на титульной

POST /api/login                         — вход (rate limiting)
GET  /api/me                            — текущая сессия
POST /api/logout                        — выход
GET  /api/departures[?all=1]            — заезды (all=1 только для оператора)
GET  /api/tours                         — каталог туров
GET  /api/bookings                      — брони агентства
POST /api/bookings                      — создать бронь
POST /api/bookings/:id/passengers       — 403: состав меняет оператор
POST /api/bookings/:id/change-request   — заявка оператору на замену
POST /api/bookings/:id/cancel-request   — заявка оператору на отмену
POST /api/bookings/:id/cancel           — 403: отменяет только оператор

-- только operator:
GET  /api/admin/bookings                — все брони (фильтры: agency/departure/status/debt)
GET  /api/admin/manifest?departure_id=  — список пассажиров
GET  /api/admin/manifest?departure_id=&csv=1  — выгрузка CSV (BOM для Excel)
POST /api/admin/payments                — оплата/возврат
POST /api/admin/bookings/:id/cancel     — отменить бронь
POST /api/admin/bookings/:id/passengers — заменить состав (в два шага)
GET  /api/admin/tours                   — туры с комиссиями и числом заездов
GET  /api/admin/destinations            — плитки направлений (список из туров)
POST /api/admin/destinations/:name      — оформление плитки
POST /api/admin/media                   — загрузка фотографии (R2 или KV)
POST /api/admin/tours                   — создать тур
POST /api/admin/tours/:id               — правка тура (код не меняется)
GET  /api/admin/tours/:id/content       — контент карточки и варианты
POST /api/admin/tours/:id/content       — правка контента (целиком)
POST /api/admin/departures              — создать заезд (по образцу)
POST /api/admin/departures/:id/(open|close) — открыть/закрыть продажу заезда
POST /api/admin/departures/:id/date     — перенести заезд (в два шага)
POST /api/admin/departures/:id/prices   — правка прайса заезда (целиком)
POST /api/admin/passengers/:id/document — исправить ФИО/паспорт (без пересчёта)
POST /api/admin/passengers/:id/birthdate — исправить дату рождения (с пересчётом)
GET  /api/admin/agencies                — список агентств
POST /api/admin/agencies                — создать агентство
POST /api/admin/agencies/:id/(activate|deactivate)
POST /api/admin/agencies/:id/password
GET  /api/admin/bookings/:id/history    — журнал (возвращает actor_role)
```

## Деплой (пошагово)

```bash
# 1. Тест локально (нужен wrangler login заранее)
cd test && ./run.sh          # должно быть 135/135

# 2. Создать D1 базу
cd worker
wrangler d1 create turon-tour
# скопировать database_id в wrangler.toml

# 3. Сгенерировать seed (пароли печатаются один раз в консоль — сохранить!)
python3 tools/build-seed.py > db/seed.sql

# 4. Залить в D1
wrangler d1 execute turon-tour --remote --file=../db/schema.sql
wrangler d1 execute turon-tour --remote --file=../db/seed.sql

# 5. Задеплоить воркер
wrangler deploy

# 6. Вписать URL воркера
# js/config.js: window.TURON_CONFIG = { apiBaseUrl: "https://turon-tour-api.XXXX.workers.dev" }

# 7. Вписать URL кабинета в ALLOWED_ORIGIN
# worker/wrangler.toml: ALLOWED_ORIGIN = "https://amalsabitov98-art.github.io"

# 8. GitHub Pages: Settings → Pages → Deploy from branch → main / root
```

## Правки уже работающей базы
`db/seed.sql` для этого НЕ подходит (стирает агентства и заезды). Только миграции:

```bash
cd worker
wrangler d1 execute turon-tour --remote --file=../db/migrations/005-catalog-refresh.sql
```

`006-destination-image.sql` — фотография направления «Турция и Грузия»
(`destinations.image`). Тоже идемпотентна, заезды не трогает.

`007-extra-bed.sql` — доп. кровать `DBLX` в прайс всех заездов, цена берётся
из `TRPL` того же заезда (временная, см. раздел о предварительных данных).
Идемпотентна: сначала удаляет свои строки, потом вставляет заново, поэтому
её же можно прогнать повторно после смены цены. Уже созданные брони не
меняются — цена зафиксирована в `passengers.price` на момент брони и из
`departure_prices` не перечитывается.

`015-capacity-soft.sql` — снимает `CHECK (seats_taken <= capacity)` со
старых баз. В коде потолок убран давно (вместимостью управляет оператор
вручную), а в схеме остался: 66-й пассажир ронял бронь ошибкой БД, потому
что `UPDATE departures` не обёрнут в try/catch. **НЕ идемпотентна** —
перестраивает таблицу через `CREATE ... INSERT ... DROP ... RENAME`.

Перед запуском обязателен бэкап:
```bash
cd worker
wrangler d1 export turon-tour --remote --output=backup.sql
wrangler d1 execute turon-tour --remote --file=../db/migrations/015-capacity-soft.sql
```

Две мины, на которых уже подорвались при её отладке:
- `PRAGMA defer_foreign_keys` откладывает CHECK, но **НЕ откладывает
  CASCADE**: `DROP TABLE departures` каскадом снёс весь `departure_prices`
  (1 → 0 строк), а COMMIT всё равно упал. Нужен именно
  `PRAGMA foreign_keys = OFF`.
- Порядок «переименовать старую, потом создать новую» не работает: SQLite
  при `RENAME` переписывает ссылки FK в ДРУГИХ таблицах, и они начинают
  указывать на `departures_old`. Только «создать `departures_rebuild` →
  перелить → `DROP` старую → `RENAME`».

После прогона проверить, что цены и заезды на месте:
```sql
SELECT COUNT(*) FROM departures; SELECT COUNT(*) FROM departure_prices;
```

`005` идемпотентна и заменяет собой 002/003/004: наполняет направления,
варианты маршрута, программу по дням, включено/не включено, «Информация»,
описание и длительность туров. Заезды, места и агентства не трогает.

## Демо-логины (только браузерный демо, не БД)
`umida`, `easytourism`, `ofotour`, `operator` — пароль `turon2026`

## Что ещё не сделано (TODO.md)
- Фотографий туров нет ни одной: `tour_content` с `kind='gallery'` и
  `destinations.image` поддерживаются и рисуются, но данных нет — ждём оператора
- Самостоятельная регистрация агентства заявкой с юр. данными и модерацией
- Разбивка цены на компоненты (страховка, транспорт, гид, билет, отель)
- Бронирование от имени агентства (оператор берёт заявку по телефону)
- Переименование агентства
- Правка примечания к брони
- Ручная правка остатка мест (нет сверки с реальностью)
- Квартальные бонусы (не ясны правила — см. TODO.md)
- Мультивалютность — только USD
- Перевод КАБИНЕТА (678 строк) — сознательно не сделан, см. раздел о языках

## Известные нюансы / подводные камни
- `[hidden]{display:none!important}` в CSS — без этого `hidden` не работает, если элемент имеет `display` от другого правила
- История брони возвращает `actor_role` (JOIN с agencies) — чтобы тест не сравнивал кириллицу (падало на Windows Git Bash)
- `build-seed.py` использует `Path(__file__).resolve().parent.parent` для путей — запускать можно из любой папки
- CSV-выгрузка: разделитель `;`, BOM `﻿` в начале — для Excel с кириллицей
- Пароли генерируются случайно при каждом запуске `build-seed.py` и больше нигде не хранятся
- Вместимость заездов: 65 для всех (не указана в ведомости), уточнить у оператора
- Детский тариф `Chd 5-11` сменился на `Chd 5-10` с 17.07 — в данных оба варианта, верхняя граница не включается
- `js/seed-data.js` действительно генерируется `build-seed.py` (раньше файл это утверждал, но генератор его не писал — расхождение устранено)
- **ГЛАВНАЯ ЛОВУШКА: тесты не проверяют `db/seed.sql`.** `test/reset-db.sh`
  пересобирает наполнение через `build-seed.py`, а в боевую базу заливается
  файл `db/seed.sql`. Они разошлись, и в проде не оказалось ни направлений,
  ни вариантов маршрута, ни программы по дням (14 строк), ни `tours.nights` —
  при 102/102 зелёных тестах. Симптом был «длительность уточняется».
  Лечение прода — `db/migrations/005-catalog-refresh.sql` (только контент,
  заезды и агентства не трогает). Защита — `tools/check-seed.py`, вызывается
  из `test/run.sh`. После правки `build-seed.py` ОБЯЗАТЕЛЬНО пересобрать:
  `python3 tools/build-seed.py > db/seed.sql` (пароли печатаются один раз)
- `db/seed.sql` — файл ПЕРВОГО наполнения пустой базы: он делает
  `DELETE FROM agencies/departures/tours`. На работающей базе запускать нельзя —
  слетят пароли агентств, а брони останутся без заездов. Правки живой базы
  только через `db/migrations/`
- `test/reset-db.sh` пересоздаёт справочники через DROP: `schema.sql` работает через `CREATE TABLE IF NOT EXISTS`, поэтому на старой локальной базе новые колонки сами не появятся
- `BUS` — это код аэропорта **Батуми**, а не «автобус» (подтверждено билетами Centrum Air: TAS→BUS, BUS→TAS). `TZX` — Трабзон. Расхождение цен «05.06 автобус 630 / авиа 600» на самом деле Батуми/Трабзон
- Фотографии лежат в `img/`, а не base64 в CSS: со встроенными картинками `styles.css` весил 510 КБ и блокировал отрисовку (на 3G первый кадр 3.7 с против 1.6 с). `build-preview.js` возвращает их внутрь — превью должно оставаться одним файлом
- Шрифты Google подключены через `rel="preload"` + `onload`, а не обычным `link rel=stylesheet`: при недоступном fonts.googleapis.com блокирующий вариант держал первый кадр 5 секунд
- Боковое меню на ширине ≤860px выдвижное: кнопка `#nav-toggle` + класс `is-nav-open` на `#screen-app`. Без этого колонка 248px не помещалась в экран телефона
- В карточке тура Карадениз ДВА варианта маршрута (прилёт в Батуми / в Трабзон) —
  это физически разные маршруты. Привязка к заезду ЖЁСТКАЯ (подтверждено
  оператором): `BUS` — прилёт в Батуми, `TZX` — прилёт в Трабзон. Переключатель
  в UI остался (карточка тура показывает даты обоих аэропортов сразу), но при
  открытии расчёта на конкретной дате программа переключается на её маршрут
  сама — `variantForDeparture` в `js/catalog.js`. Вариант определяется по ТЕКСТУ
  заголовка («прилёт в …»), а не по коду A/B: код — только порядок сортировки
- Авиабилет Ташкент — Трабзон/Батуми входит в цену заезда (подтверждено оператором) — стоит в «включено», а не в «не включено»
- `BUS` — IATA-код аэропорта Батуми, а не автобус. В интерфейсе показывается как «Авиа · Батуми»; `TZX` — «Авиа · Трабзон».
- PDF-программы туров (55 и 39 МБ) лежат на Google Диске оператора, а не в
  репозитории: ссылки в `js/provisional.js` → `PROGRAMS`, по одной на
  направление. Файлы должны быть открыты «для всех, у кого есть ссылка»
- Глобал публичной шапки называется `TuronPublicUi` (со строчной `i`), а не
  `TuronPublicUI` — из `js/app.js` язык переключается через него
- Проверять правки удобно на `preview.html`: `node tools/build-preview.js`,
  затем открыть файл в браузере (демо-режим, логины из раздела ниже)
- **У `<button>` в файле нет сброса фона** — глобальное правило задаёт только
  `font-family: inherit`. Кнопки без явного `background` (бренд в шапке,
  неактивные вкладки поднавигации) получают браузерный серый `ButtonFace`, а
  без явного `color` — чёрный `ButtonText`. На кремовой теме это сливалось с
  фоном и было незаметно; на тёмной вылезло серыми плашками и пропавшим
  текстом. В публичной части починено точечно. **Если будете красить тёмным
  что-то ещё — проверяйте каждую кнопку, мина та же.**
- **МИНА: `styles.css` уже один раз обрезали.** Коммит `caacd9e` («Style all
  Umrah tour pages…») показал в статистике `24 insertions, 954 deletions` —
  у файла срезало весь хвост, блок `PARTNER CABINET POLISH` (оформление
  сайдбара, верхней панели, аккаунта, кнопки выхода, пиктограмм вкладок,
  колокольчика и галереи билдера). Последнее правило осталось незакрытым.
  Внешне: иконки без размеров разворачивались во весь блок — кнопка
  сворачивания меню стала квадратом 210×210 с чёрным треугольником, «Выход»
  превратился в чёрную плиту. Разметка при этом на месте, JS не падает, и
  ВСЕ тесты были зелёными.
  Восстановлено из `caacd9e^`. Защита — два теста в блоке «Кабинет
  агентства»: ни одна иконка в `#screen-app` не крупнее 64px, и
  `#sidebar-collapse` имеет размер из правила, которое лежит в самом конце
  файла. Проверено отключением: на обрезанном файле оба падают.
  **Правя styles.css целиком (через запись файла, а не Edit) — сверяйте
  число строк до и после.**
- **Мина, на которой уже подорвались:** в `styles.css` годами лежали четыре
  мёртвых блока палитры (`:root` со старой схемой, `@media
  (prefers-color-scheme: dark)` и `:root[data-theme="dark"/"light"]`). Их
  перекрывал настоящий `:root` ниже, поэтому они молчали. Как только
  `data-theme` стал осмысленным (кнопка день/ночь), блок
  `:root[data-theme="dark"]` ожил — его специфичность (0,2,0) выше обычного
  `:root` (0,1,0) — и перекрасил **весь сайт вместе с кабинетом** в старую
  коричневую палитру. Блоки удалены; палитру задаёт единственный `:root`.
  **Если снова появится правило на `:root[...]` — помните, что оно бьёт
  базовый `:root` независимо от порядка строк**
- `--tt-forest` в тёмной публичной теме стал почти чёрным. Всё, что красило
  им ТЕКСТ (крошки, «О компании», стрелка на плитке), пришлось чинить
  отдельно — при правках палитры проверяйте `color: var(--tt-forest)`
- **Chromium из playwright собран без H.264** — оба `.mp4` в нём не
  проигрываются (`readyState` 0, `videoWidth` 0), скриншот показывает
  `poster`. Это не поломка вёрстки: постеры нарезаны из самих роликов, так
  что кадр на скриншоте представителен. Проверять же само воспроизведение в
  этом окружении нельзя — только у себя в браузере
- Видео титульной перекодировано из настоящего 4K-мастера (Travel_02.mov,
  663 МБ, HandBrake на стороне оператора): `2560×1440, 25 fps, H.264, RF 21,
  slow, faststart, без звука`. До этого несколько раз пересжимали чужую уже
  сжатую веб-копию (1920×1080, 9,9 МБ) — сколько ни понижай CRF, выше
  резкости исходника не прыгнуть, детали не вернуть. Мастер лежал на Google
  Диске (663 МБ) — не поместился ни через прямой доступ (прокси режет
  drive.google.com), ни через Drive-коннектор (663 МБ в base64 ≈ 880 МБ
  текста, не проходит по размеру ни в одном канале). Решилось прогоном
  HandBrake на стороне оператора и загрузкой готового результата файлом.
  Постер (`hero-travel-poster.jpg`, 320 КБ) держит первый кадр. При замене
  исходника кроп центра (глобус) в масштабе — линии графики первыми выдают
  пересжатие
- **Ролик обрезан в бесшовную петлю**, а не крутится все 22 секунды: у
  вступления (fade-in из сплошного цвета + сборка колец) нет пары для
  склейки, поэтому используется только устоявшаяся фаза вращения. Точка
  петли найдена перебором — сравнивали MSE уменьшенных кадров якоря
  (t≈4-5с, когда все иконки уже полностью прорисованы) со всем хвостом
  ролика; несколько независимых пар сошлись на цикле ~14.2с, значит это
  и есть родной период анимации. Между t=5.10 и t=19.30 конец подшит к
  началу кроссфейдом 0.8с (`ffmpeg xfade`, а не жёсткая склейка): позиции
  колец совпадают не пиксель-в-пиксель, и без плавного перехода был бы
  заметен рывок. На статичном кадре ровно в середине бленда неизбежно
  видно лёгкое двоение контуров — это нормально для кроссфейда и не
  проверяется скриншотом, только живым проигрыванием.

  **Вторым входом в `xfade` идёт ГОЛОВА петли, а не продолжение хвоста.**
  Сначала было наоборот (`trim=19.30:20.10`), и петля молча не сходилась:
  последний кадр оказывался фазой t=20.10, первый — t=5.10, разрыв 0.8с,
  то есть каждые 14 секунд ролик дёргался. Правильно `trim=4.30:5.10` —
  тогда бленд уводит хвост ровно в тот кадр, с которого петля начинается:
  ```
  [0:v]trim=5.10:19.30,setpts=PTS-STARTPTS,fps=25[a];
  [0:v]trim=4.30:5.10,setpts=PTS-STARTPTS,fps=25[b];
  [a][b]xfade=transition=fade:duration=0.8:offset=13.4[v]
  ```
  Проверяется MSE уменьшенных кадров: шов (последний→первый) должен быть
  на уровне соседних кадров. Было 397 при 26 у соседних, стало 33 при 32.
  `fps=25` в каждой ветке обязателен — `xfade` требует постоянный fps.
  Глазами в этом окружении не проверить: Chromium без H.264
- **Полей в файле видео БОЛЬШЕ НЕТ — не возвращать.** Была попытка дать
  `object-fit: cover` запас под обрезку прямо в кадре: фон вокруг кольца
  масштабировался, размывался (`gblur`) и подкладывался под оригинал
  (`overlay`) — сначала по 260px сверху и снизу, потом по 110px
  (кадр 2560×1660). Кольцо переставало обрезаться, но появилась ровно та
  беда, ради которой всё это и переделывалось: размытая подложка читается
  на экране плоской полосой, а её граница с настоящим кадром — чёткой
  горизонтальной линией сверху и снизу героя. Никаким CSS это не лечится,
  линия внутри самого видео. Файл вернулся в родной кадр 2560×1440
  (`crop=2560:1440:0:110` от старой версии либо просто пересборка петли
  из мастера без `overlay`), постер пересобран из нового первого кадра.

  Настоящее решение задачи «кольцо целиком + видео во весь экран без
  полей» — не только в файле видео, но и в высоте блока. `.tt-public-intro`
  выше окна (`145svh`, не `100svh`): чем выше блок относительно ширины
  экрана, тем меньше `cover` обрезает видео по вертикали — при достаточной
  высоте обрезка вообще уходит в горизонталь, где у кольца огромный запас
  (~23% против ~9-10% по вертикали). 145svh держит кольцо целым вплоть до
  честного 21:9 ultrawide без обрезки вообще — этого достаточно и без
  подстраховки полями в файле, поэтому поля и убраны.

  Ловушка: если центрировать текст и поиск по всей высоте 145svh (как было
  через `justify-content:center` на самом `.tt-public-intro`), контент
  уезжает на середину блока — то есть НИЖЕ первого экрана, посетитель
  открывает страницу и не видит ни заголовка, ни поиска без скролла.
  Решение — обёртка `.tt-hero-content` (в `js/catalog.js`, дочерний блок
  внутри `.tt-public-intro`, вокруг `.tt-public-hero-copy` и
  `searchPanelHtml()`): она держит `min-height:100svh` и центрирование
  сама по себе, в границах одного экрана, а `.tt-public-intro` просто
  выше — лишние 45svh остаются пустым продолжением видео ниже, его
  открывает прокрутка. `.tt-hero-video` остаётся ВНЕ обёртки, прямым
  потомком `.tt-public-intro` — растягивается на весь блок (все 145svh),
  а не только на первый экран. На мобильных `.tt-hero-content` получает
  `min-height:auto` — там высоту и так задаёт контент (шапка + заголовок +
  поиск), закреплять лишний пол незачем
- `mp4` по `file://` Chromium не тянет вообще — превью с видео смотреть через
  `python3 -m http.server`, иначе не загрузится даже там, где кодек есть
- `img/hero-rize-batumi.webp`, `hero-rize-morning.webp`, `hero-batumi-sunset.webp`
  осели без ссылок после замены карусели на видео. Не удалены намеренно: это
  единственные живые фотографии направлений, а фотографий туров у нас нет
  (см. TODO). В браузер они не грузятся — на скорость не влияют

## Проверка интерфейса
```bash
npm install                     # один раз: playwright (единственная зависимость)
node tools/build-preview.js     # пересобрать превью из исходников
node test/ui-test.mjs           # 475 тестов: навигация, темы, титульная, обе роли, бронь, телефон
```
`package.json` нужен только для тестов — сам сайт по-прежнему без сборки,
в браузер уезжают исходные файлы как есть. Нужен playwright и Chromium. Тест гоняет `preview.html` в демо-режиме, поэтому
бэкенд поднимать не нужно. Прогонять после любой правки интерфейса — именно
эти тесты ловят «висячие» ссылки на удалённые элементы.
